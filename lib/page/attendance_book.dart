import 'package:flutter/material.dart';

class AttendanceBook extends StatelessWidget {
  const AttendanceBook({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
