import 'package:flutter/material.dart';

class DecideTimeTable extends StatelessWidget {
  const DecideTimeTable({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}
